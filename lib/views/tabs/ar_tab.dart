import 'package:flutter/material.dart';

class ARTab extends StatelessWidget {
  const ARTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("AR Camera", style: TextStyle(fontSize: 30)),
    );
  }
}