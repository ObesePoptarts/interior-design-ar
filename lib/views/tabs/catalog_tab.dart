import 'package:flutter/material.dart';

class CatalogTab extends StatelessWidget {
  const CatalogTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Catalog", style: TextStyle(fontSize: 30)),
    );
  }
}