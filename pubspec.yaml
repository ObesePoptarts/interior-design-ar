name: interior_design_ar
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.12.0+1
  provider: ^6.1.5+1  

  firebase_core: ^4.7.0 
  firebase_storage: ^13.3.0 
  cloud_firestore: ^6.3.0

  permission_handler: ^12.0.1
  
  vector_math: ^2.2.0
  
  file_picker: ^11.0.2
  ar_flutter_plugin_plus: ^1.1.3
  





dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:
  uses-material-design: true

assets:
  - assets/logo.png